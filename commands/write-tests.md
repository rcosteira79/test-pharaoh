---
description: "Write tests for the current Android branch driven by user story + ACs. Adheres to the project's existing conventions; never generates mock-based tests."
---

The user invoked `/write-tests`. Invoke the Skill tool with skill name `android-test-agent:write-tests` immediately and follow that workflow in order, without skipping gates (Preflight, Gate 0 Feature understanding, Gate 1 Plan review, Gate 2 Escalation).
